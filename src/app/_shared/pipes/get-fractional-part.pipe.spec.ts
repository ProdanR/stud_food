import { GetFractionalPartPipe } from './get-fractional-part.pipe';

describe('GetFractionalPartPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFractionalPartPipe();
    expect(pipe).toBeTruthy();
  });
});
